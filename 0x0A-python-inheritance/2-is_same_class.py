#!/usr/bin/python3
"""This documentation is always neccessary"""


def is_same_class(obj, a_class):
    """A functions to check if an object is an instance of a class"""
    if (type(obj) == a_class):
        return True
    return False
